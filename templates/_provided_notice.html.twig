{#
Suggestion shown when a built-in (provided) template renders. `_crud_engine` is
supplied by ViewRenderer only on the fallback path.
#}
<div class="crud-engine-provided-notice">
    <p>This built-in template was provided for entity <code>{{ _crud_engine.entityClass }}</code> and action <code>{{ _crud_engine.action }}</code>.</p>
    <p>Create file <code>{{ _crud_engine.viewPath }}</code> in your template directory (usually in <code>templates/</code>) to customize rendering.</p>
</div>
